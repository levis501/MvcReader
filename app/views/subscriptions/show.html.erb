<%= link_to "Sign in as a different user", users_signin_path %>
<%= link_to "Go Back", user_path(@subscription.user_id) %>
<hr>

<p id="notice"><%= notice %></p>

<%= render :partial => "rss_feeds/rss_feed_header", :locals => { :rss_feed_id => @subscription.rss_feed_id, :user_id => @subscription.user_id } %>
<hr>
<%= render :partial => "rss_feeds/feed", :locals => { :rss_feed_id => @subscription.rss_feed_id, :user_id => @subscription.user_id } %>

<%= link_to 'Edit', edit_subscription_path(@subscription) %> |
<%= link_to 'Back', subscriptions_path %>
