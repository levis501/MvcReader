<%= form_for(@post_read_record) do |f| %>
  <% if @post_read_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post_read_record.errors.count, "error") %> prohibited this post_read_record from being saved:</h2>

      <ul>
      <% @post_read_record.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :references %><br />
    <%= f.text_field :references %>
  </div>
  <div class="field">
    <%= f.label :references %><br />
    <%= f.text_field :references %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
